{% for name, item in __nagiosconfig_timeperiods.items() if name not in __nagiosconfig_reserved.timeperiods | default([]) %}
define timeperiod {
    name   {{ name }}
    timeperiod_name   {{ name }}
{% for key, value in item.items() %}
    {{ key }}   {{ value }}
{% endfor %}
}
{% endfor %}
